name: Build wheels

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            arch: x86_64
          - os: macos-14
            arch: arm64
          - os: macos-15
            arch: x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build deps (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libreadline-dev zlib1g-dev

      - name: Install build deps (macOS)
        if: runner.os == 'macOS'
        run: |
          xcode-select --install 2>/dev/null || true

      - name: Build PostgreSQL + pgvector
        run: |
          cd pgbuild && make -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)

      - name: Build wheel
        run: |
          pip install build setuptools wheel
          python -m build --wheel

      - name: Fix Linux wheel tag
        if: runner.os == 'Linux'
        run: |
          pip install wheel
          for f in dist/*-linux_*.whl; do
            python -m wheel tags --platform-tag manylinux_2_28_x86_64 --remove "$f"
          done

      - name: Test
        run: |
          pip install dist/*.whl
          pip install pytest psycopg2-binary
          pytest tests/ -v

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-${{ matrix.arch }}
          path: dist/*.whl

  publish:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
